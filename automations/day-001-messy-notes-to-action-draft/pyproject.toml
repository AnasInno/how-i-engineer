[project]
name = "day_001_messy_notes_to_action_draft"
version = "0.1.0"
description = "Daily AI automation: Messy notes to action draft"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
